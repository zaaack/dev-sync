{
  "name": "@zaaack/scp-sync",
  "version": "0.1.6",
  "description": "Sync files to dev macines via scp",
  "main": "./lib/index.js",
  "repository": "https://github.com/zaaack/scp-sync",
  "author": "zaaack",
  "license": "MIT",
  "bin": {
    "scp-sync": "./lib/cli.js"
  },
  "files": [
    "lib"
  ],
  "scripts": {
    "start": "tsc -w",
    "build": "rm -rf ./lib && tsc"
  },
  "dependencies": {
    "foy": "^0.2.1",
    "minimatch": "^3.0.4"
  },
  "devDependencies": {
    "@types/minimatch": "^3.0.3",
    "@types/node": "^14.14.32",
    "typescript": "^4.2.3"
  }
}
